---
interface Props {
  name: string;
  slug: string;
}

const { name, slug } = Astro.props;
---

<a
  href={`/documentacion/categoria/${slug}`}
  class="inline-block py-1 px-5 bg-coffee-600 hover:bg-coffee-500 text-sm text-white mr-2 rounded"
>
  {name}
</a>
