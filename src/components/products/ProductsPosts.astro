---
import { ProductsSchema } from "@/types";
import PostCard from "./PostCard.astro";
const url = `${import.meta.env.API_URL}/products`;
const res = await fetch(url);
const json = await res.json();
const data = ProductsSchema.parse(json);
console.log(data);
---

{data.map((post) => <PostCard post={post} />)}
